import { IframeWrapperDirective } from './iframe-wrapper.directive';

describe('IframeWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
